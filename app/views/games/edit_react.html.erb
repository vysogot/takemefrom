<!DOCTYPE html>
<html>
  <head>
    <title><%= @game.name %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= javascript_pack_tag 'game_editor' %>
  </head>
  <body>
    <div id="editor" data-elements="<%= @nodes.concat(@edges).to_json %>">
    </div>
  </body>
</html>
